## Learned User Preferences
- User prefers iterative debugging that continues until build/lint/runtime checks are fully passing.
- User repeatedly asks the agent to run the app directly and verify it is actually reachable in the browser.
- User wants changes implemented exactly to provided specs when they paste exact dependency manifests.
- User does not want Git/commit workflow requirements included in project setup instructions.

## Learned Workspace Facts
- Workspace project is a Vite + React + TypeScript web app.
- Dev server commonly runs on port 5173 for local access.
- The codebase has wallet and crypto-related modules that require additional runtime dependencies beyond a minimal React-only manifest.
